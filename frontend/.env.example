# Base URL of the AR Medical backend API (include /api).
# In local dev, Vite proxies /api to the backend, so this can stay relative.
VITE_API_BASE_URL=/api

VITE_BUSINESS_NAME="AR Medical"
VITE_BUSINESS_ADDRESS="Wadi e Huda, Shimoga 572100"
